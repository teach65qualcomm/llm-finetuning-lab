# Prompt 2 — Create the Implementation Plan
Based on your assessment, create a detailed implementation plan. No code yet.

Order: llm_config → retriever → drafter → reviewer → supervisor → persistence
For each task: file to modify, specification to follow, focused test command, failure path to prove.
Do not combine tasks or suggest doing them out of order.
